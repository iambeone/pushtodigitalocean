name: 'Push to DigitalOcean'
description: 'Make a Digital Ocean droplet and push docker image in it'
inputs:
  file:  # id of input
    description: 'name of the file'
    required: false
    default: 'dockerimage12.tgz'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - ${{ inputs.file }}
